---

const { title, description, image, link } = Astro.props;
---
<div class="max-w-sm mx-auto rounded-lg overflow-hidden shadow-lg bg-white transition-shadow duration-300 hover:shadow-2xl">
  <img class="w-full h-48 object-cover transition-transform duration-500 transform hover:scale-105" src={image} alt={title} />
  <div class="p-4 text-center">
    <div class="font-bold text-xl text-gray-800 mb-2">{title}</div>
    <p class="text-gray-600 text-base mb-4">{description}</p>
  </div>
  <div class="p-4 text-center">
    <a href={link} class="inline-block bg-viajar-4 hover:bg-viajar-2 text-white font-bold py-2 px-6 rounded transition-all duration-500 ease-in-out transform hover:scale-105 hover:shadow-lg">
      Ver Proyecto
    </a>
  </div>
</div>